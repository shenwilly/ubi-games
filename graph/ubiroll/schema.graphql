type Bet @entity {
  id: ID!
  player: Bytes!
  chance: BigInt!,
  amount: BigInt!,
  prize: BigInt,
  result: BigInt,
  requestId: Bytes!,
  win: Boolean,
  finished: Boolean!,
  timestamp: BigInt!,
  txHash: Bytes!,
}
